# Parent Data Force — Docling Lab API rate limiting (http context)
# Zone shared by dev + tools vhosts; limits /api/docling/* to 3 req/min
# per client IP with a burst of 2. Excess requests get 503.
limit_req_zone $binary_remote_addr zone=docling:10m rate=3r/m;
